apiVersion: v1
kind: Service
metadata:
  name: full-stack-service
spec:
  selector:
    app: full-stack-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
